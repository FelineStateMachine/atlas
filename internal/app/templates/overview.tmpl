{{/* overview — the corner map: the whole world, and where the camera is. */}}
{{define "overview"}}<div class="overview">{{with .Volume}}<span class="overview-world">{{.World.Title}}</span>{{end}}</div>
{{end}}
