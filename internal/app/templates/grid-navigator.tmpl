{{/* grid-navigator — cell system cycling, the held cell, the subgrid. */}}
{{define "grid-navigator"}}<div class="grid-navigator" data-system="{{.Session.Grid.System}}" data-cell="{{.Session.Grid.Cell}}" data-subgrid="{{.Session.Grid.Subgrid}}"></div>
{{end}}
